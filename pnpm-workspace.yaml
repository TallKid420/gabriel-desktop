packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  sharp: true
  unrs-resolver: true

# Native deps allowed to run install/build scripts (Next image optimizer +
# the ESLint resolver). Everything else stays sandboxed.
onlyBuiltDependencies:
  - sharp
  - unrs-resolver
